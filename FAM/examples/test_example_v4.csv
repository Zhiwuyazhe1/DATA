namespace,class,func,docstring,taint_summary,memory_summary
"std::vector<T,Allocator>::vector",vector,vector() : vector(Allocator()) {}," C++11 起的默认构造函数。构造拥有默认构造的分配器的空 vector。
 如果 Allocator 不可默认构造 (DefaultConstructible) ，那么行为未定义。参数
alloc	-	用于此容器所有内存分配的分配器",{},"{
    <0>.size = 0
}"
"std::vector<T,Allocator>::vector",vector,vector() noexcept(noexcept(Allocator())) : vector(Allocator()) {}," C++11 起的默认构造函数。构造拥有默认构造的分配器的空 vector。
 如果 Allocator 不可默认构造 (DefaultConstructible) ，那么行为未定义。参数
alloc	-	用于此容器所有内存分配的分配器",{},"{
    <0>.size = 0
}"
"std::vector<T,Allocator>::vector",vector,explicit vector( const Allocator& alloc = Allocator() );,"C++11 前的默认构造函数。构造拥有给定分配器 alloc 的空 vector。参数
alloc	-	用于此容器所有内存分配的分配器",{},"{
    <0>.size = 0
}"
"std::vector<T,Allocator>::vector",vector,explicit vector( const Allocator& alloc );,"C++11 前的默认构造函数。构造拥有给定分配器 alloc 的空 vector。参数
alloc	-	用于此容器所有内存分配的分配器",{},"{
    <0>.size = 0
}"
"std::vector<T,Allocator>::vector",vector,"explicit vector( size_type count,
                 const Allocator& alloc = Allocator() );","构造拥有 count 个默认插入的 T 对象的 vector。不进行复制。
 如果 T 不可默认插入 (DefaultInsertable) 到 std::vector<T> 中，那么行为未定义。参数
alloc	-	用于此容器所有内存分配的分配器
count	-	容器的大小","{
  setSink(<1>)
}","{
    <0>.size = <1>
}"
"std::vector<T,Allocator>::vector",vector,"explicit vector( size_type count, const T& value = T(),
                 const Allocator& alloc = Allocator() );"," 构造拥有 count 个值 value 的元素的 vector。
如果 T 不可复制插入 (CopyInsertable) 到 std::vector<T> 中，那么行为未定义。参数
alloc	-	用于此容器所有内存分配的分配器
count	-	容器的大小
value	-	以之初始化容器元素的值","{
  setSink(<1>)
  transitive(<0>, <2>)
}","{
    <0>.size = <1>
    <0>.field = <2>
}"
"std::vector<T,Allocator>::vector",vector,"vector( size_type count, const T& value,
        const Allocator& alloc = Allocator() );"," 构造拥有 count 个值 value 的元素的 vector。
如果 T 不可复制插入 (CopyInsertable) 到 std::vector<T> 中，那么行为未定义。参数
alloc	-	用于此容器所有内存分配的分配器
count	-	容器的大小
value	-	以之初始化容器元素的值","{
  setSink(<1>)
  transitive(<0>, <2>)
}","{
    <0>.size = <1>
    <0>.field = <2>
}"
"std::vector<T,Allocator>::vector",vector,"template< class InputIt >
vector( InputIt first, InputIt last,

        const Allocator& alloc = Allocator() );"," 以范围 [first, last) 的内容构造 vector。[first, last) 中的每个迭代器都只会解引用一次。
如果 InputIt 不满足老式输入迭代器 (LegacyInputIterator) ，那么就会改为以实参 static_cast<size_type>(first)、last 和 alloc 调用重载 (4)。

(C++11 前)
此重载只有在 InputIt 满足老式输入迭代器 (LegacyInputIterator) 时才会参与重载决议。

如果满足以下任意条件，那么行为未定义：

T 从 *first 不可就位构造 (EmplaceConstructible) 到 std::vector<T> 中。
Iter 不满足老式向前迭代器 (LegacyForwardIterator) ，并且 T 不可移动插入。参数
alloc	-	用于此容器所有内存分配的分配器
count	-	容器的大小
value	-	以之初始化容器元素的值
first, last	-	要从中复制元素的元素范围的迭代器对 (MoveInsertable) 到 std::vector<T> 中。","{
  transitive(<0>, <1> || <2>)
}","{
  <0>.size = [0, MAX]
  <0>.field = <1>.field
}"
"std::vector<T,Allocator>::vector",vector,vector( const vector& other );,"以 other 的内容构造 vector。
7) 复制构造函数。
如同通过调用 std::allocator_traits<Allocator>::
    select_on_container_copy_construction
        (other.get_allocator()) 获得分配器。 other	-	用作初始化容器元素来源的另一容器","{
  transitive(<0>, <1>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
}"
"std::vector<T,Allocator>::vector",vector,vector( vector&& other );,"移动构造函数。如同通过从 other.get_allocator() 移动构造获得分配器。other	-	用作初始化容器元素来源的另一容器","{
  transitive(<0>, <1>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
}"
"std::vector<T,Allocator>::vector",vector,"vector( const vector& other, const Allocator& alloc );","与构造函数相同，但是会将 alloc 用作分配器。
如果 T 不可复制插入 (CopyInsertable) 到 std::vector<T> 中，那么行为未定义。
other	-	用作初始化容器元素来源的另一容器","{
  transitive(<0>, <1>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
}"
"std::vector<T,Allocator>::vector",vector,"vector( vector&& other, const Allocator& alloc );","与移动函数相同，但是会将 alloc 用作分配器。
 如果 T 不可移动插入 (MoveInsertable) 到 std::vector<T> 中，那么行为未定义。 other	-	用作初始化容器元素来源的另一容器","{
  transitive(<0>, <1>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
}"
"std::vector<T,Allocator>::vector",vector,"vector( std::initializer_list<T> init,
        const Allocator& alloc = Allocator() );","等价于 vector(il.begin(), il.end(), alloc)。init	-	用作初始化元素来源的初始化器列表","{
  transitive(<0>, <1>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
}"
"std::vector<T,Allocator>::~vector",vector,~vector();,析构函数。 销毁 vector。调用各元素的析构函数，然后解分配所用的存储。注意，若元素是指针，则不销毁所指向的对象。,"{
  sanitize(<0>)
}","{
    <0> = NULL
}"
"std::vector<T,Allocator>::operator=",vector,vector& operator=( const vector& other );,"复制赋值运算符。以 other 内容的副本替换内容。
若 std::allocator_traits<allocator_type>::propagate_on_container_copy_assignment::value 为 true，则用 other 的分配器的副本替换 *this 的分配器。若 *this 的分配器在赋值后将与其旧值比较不相等，则用旧分配器解分配内存，然后在复制元素前用新分配器分配内存。否则，在可行时可能复用 *this 所拥有的内存。任何情况下，原属于 *this 的元素要么被销毁，要么被逐元素复制赋值所替换。参数
other	-	用作数据源的另一容器 。返回值
*this","{
  transitive(<0>, <1>)
  transitive(<-1>, <0>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
    <-1> = <0>
}"
"std::vector<T,Allocator>::operator=",vector,vector& operator=( vector&& other );,"移动赋值运算符。用移动语义以 other 的内容替换内容（即从 other 移动 other 中的数据到此容器中）。之后 other 处于合法但未指定的状态。
 若 std::allocator_traits<allocator_type>::propagate_on_container_move_assignment::value 为 true，则用 other 的分配器的副本替换 *this 的分配器。若它为 false 且 *this 与 other 的分配器比较不相等，则 *this 不能接管 other 所拥有的内存的所有权且必须单独地移动赋值每个元素，并用其自身的分配器按需分配额外内存。任何情况下，原属于 *this 的元素要么被销毁，要么被逐元素移动赋值所替换。 参数
other	-	用作数据源的另一容器
ilist	-	用作数据源的 initializer_list
返回值
*this","{
  transitive(<0>, <1>)
  transitive(<-1>, <0>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
    <-1> = <0>
}"
"std::vector<T,Allocator>::operator=",vector,vector& operator=( vector&& other ) noexcept,"移动赋值运算符。用移动语义以 other 的内容替换内容（即从 other 移动 other 中的数据到此容器中）。之后 other 处于合法但未指定的状态。
 若 std::allocator_traits<allocator_type>::propagate_on_container_move_assignment::value 为 true，则用 other 的分配器的副本替换 *this 的分配器。若它为 false 且 *this 与 other 的分配器比较不相等，则 *this 不能接管 other 所拥有的内存的所有权且必须单独地移动赋值每个元素，并用其自身的分配器按需分配额外内存。任何情况下，原属于 *this 的元素要么被销毁，要么被逐元素移动赋值所替换。 参数
other	-	用作数据源的另一容器
ilist	-	用作数据源的 initializer_list
返回值
*this","{
  transitive(<0>, <1>)
  transitive(<-1>, <0>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
    <-1> = <0>
}"
"std::vector<T,Allocator>::operator=",vector,vector& operator=( std::initializer_list<value_type> ilist );,"以 initializer_list ilist 所标识者替换内容。
参数
other	-	用作数据源的另一容器
ilist	-	用作数据源的 initializer_list
返回值
*this","{
  transitive(<0>, <1>)
  transitive(<-1>, <0>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
    <-1> = <0>
}"
"std::vector<T,Allocator>::assign",vector,"void assign( size_type count, const T& value );","以 count 份 value 的副本替换内容。参数
count	-	容器的新大小
value	-	用以初始化容器元素的值","{
  setSink(<1>)
  transitive(<0>, <2>)
}","{
    <0>.size = <1>
    <0>.field = <2>
    expansion(<0>)
}"
"std::vector<T,Allocator>::assign",vector,"template< class InputIt >
void assign( InputIt first, InputIt last );","以范围 [first, last) 中元素的副本替换内容。
 其中有任何一个实参是指向 *this 中的迭代器时行为未定义。
如果 InputIt 是整数类型，那么此重载与 (1) 拥有相同效果。

(C++11 前)
此重载只有在 InputIt 满足老式输入迭代器 (LegacyInputIterator) 时才会参与重载决议。参数
count	-	容器的新大小
value	-	用以初始化容器元素的值
first, last	-	要复制的源元素范围的迭代器对","{
  transitive(<0>, <1> || <2>)
}","{
  <0>.size = [0, MAX]
  <0>.field = <1>.field
  expansion(<0>)
}"
"std::vector<T,Allocator>::assign",vector,void assign( std::initializer_list<T> ilist );," 以 ilist 的元素替换内容。
指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。参数
count	-	容器的新大小
value	-	用以初始化容器元素的值
first, last	-	要复制的源元素范围的迭代器对
ilist	-	复制值来源的 std::initializer_list","{
  transitive(<0>, <1>)
}","{
    <0>.size = <1>.size
    <0>.field = <1>.field
    expansion(<0>)
}"
"std::vector<T,Allocator>::get_allocator",vector,allocator_type get_allocator() const;,"返回与容器关联的分配器。

参数
（无）

返回值
关联的分配器。",{},{}
"std::vector<T,Allocator>::at",vector,reference at( size_type pos );,"返回位于指定位置 pos 的元素的引用，有边界检查。

若 pos 不在容器范围内，则抛出 std::out_of_range 类型的异常。

参数
pos	-	要返回的元素的位置
返回值
到所需元素的引用。","{
  setSink(<1>)
  transitive(<-1>, <0>)
}","{
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::at",vector,const_reference at( size_type pos ) const;,"返回位于指定位置 pos 的元素的引用，有边界检查。

若 pos 不在容器范围内，则抛出 std::out_of_range 类型的异常。

参数
pos	-	要返回的元素的位置
返回值
到所需元素的引用。","{
  setSink(<1>)
  transitive(<-1>, <0>)
}","{
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::operator[]",vector,reference operator[]( size_type pos );,"返回位于指定位置 pos 的元素的引用。不进行边界检查。

参数
pos	-	要返回的元素的位置
返回值
到所需元素的引用。","{
  setSink(<1>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [0, <0>.size - 1])
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::operator[]",vector,const_reference operator[]( size_type pos ) const;,"返回位于指定位置 pos 的元素的引用。不进行边界检查。

参数
pos	-	要返回的元素的位置
返回值
到所需元素的引用。","{
  setSink(<1>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [0, <0>.size - 1])
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::front",vector,reference front();,"返回到容器首元素的引用。

在空容器上对 front 的调用是未定义的。

参数
（无）

返回值
到首元素的引用","{
  transitive(<-1>, <0>)
}","{
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::front",vector,const_reference front() const;,"返回到容器首元素的引用。

在空容器上对 front 的调用是未定义的。

参数
（无）

返回值
到首元素的引用","{
  transitive(<-1>, <0>)
}","{
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::back",vector,reference back();,"返回到容器中最后一个元素的引用。

在空容器上调用 back 导致未定义行为。

参数
（无）

返回值
到最后元素的引用。","{
  transitive(<-1>, <0>)
}","{
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::back",vector,const_reference back() const;,"返回到容器中最后一个元素的引用。

在空容器上调用 back 导致未定义行为。

参数
（无）

返回值
到最后元素的引用。","{
  transitive(<-1>, <0>)
}","{
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::data",vector,T* data();,"返回指向作为元素存储工作的底层数组的指针。返回的指针使得范围 [data(), data() + size()) 始终是有效范围，即使容器为空（此时 data() 不可解引用）。

参数
（无）

返回值
指向底层元素存储的指针。对于非空容器，返回的指针与首元素地址比较相等。

复杂度
常数。

注解
如果 size() 是 ​0​，那么 data() 有可能会也有可能不会返回空指针。","{
  transitive(<-1>, <0>)
}",{}
"std::vector<T,Allocator>::data",vector,const T* data() const;,"返回指向作为元素存储工作的底层数组的指针。返回的指针使得范围 [data(), data() + size()) 始终是有效范围，即使容器为空（此时 data() 不可解引用）。

参数
（无）

返回值
指向底层元素存储的指针。对于非空容器，返回的指针与首元素地址比较相等。

复杂度
常数。

注解
如果 size() 是 ​0​，那么 data() 有可能会也有可能不会返回空指针。","{
  transitive(<-1>, <0>)
}",{}
"std::vector<T,Allocator>::begin",vector,iterator begin();,"返回指向 vector 首元素的迭代器。

如果 vector 为空，那么返回的迭代器等于 end()。参数
（无）

返回值
指向首元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::begin",vector,const_iterator begin() const;,"返回指向 vector 首元素的迭代器。

如果 vector 为空，那么返回的迭代器等于 end()。参数
（无）

返回值
指向首元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::cbegin",vector,const_iterator cbegin() const noexcept;,"返回指向 vector 首元素的迭代器。

如果 vector 为空，那么返回的迭代器等于 end()。参数
（无）

返回值
指向首元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::end",vector,iterator end();,"返回指向 vector 末元素后一元素的迭代器。

此元素表现为占位符；试图访问它导致未定义行为。参数
（无）

返回值
指向后随最后元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::end",vector,iterator end() noexcept;,"返回指向 vector 末元素后一元素的迭代器。

此元素表现为占位符；试图访问它导致未定义行为。参数
（无）

返回值
指向后随最后元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::end",vector,const_iterator end() const;,"返回指向 vector 末元素后一元素的迭代器。

此元素表现为占位符；试图访问它导致未定义行为。参数
（无）

返回值
指向后随最后元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::end",vector,const_iterator end() const noexcept;,"返回指向 vector 末元素后一元素的迭代器。

此元素表现为占位符；试图访问它导致未定义行为。参数
（无）

返回值
指向后随最后元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::cend",vector,const_iterator cend() const noexcept;,"返回指向 vector 末元素后一元素的迭代器。

此元素表现为占位符；试图访问它导致未定义行为。参数
（无）

返回值
指向后随最后元素的迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::rbegin",vector,reverse_iterator rbegin();,"返回指向逆向的 vector 的首元素的逆向迭代器。它对应非逆向 vector 的末元素。如果 vector 为空，那么返回的迭代器等于 rend()。参数
（无）

返回值
指向首元素的逆向迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::rbegin",vector,const_reverse_iterator rbegin() const;,"返回指向逆向的 vector 的首元素的逆向迭代器。它对应非逆向 vector 的末元素。如果 vector 为空，那么返回的迭代器等于 rend()。参数
（无）

返回值
指向首元素的逆向迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::crbegin",vector,const_reverse_iterator crbegin() const noexcept;,"返回指向逆向的 vector 的首元素的逆向迭代器。它对应非逆向 vector 的末元素。如果 vector 为空，那么返回的迭代器等于 rend()。参数
（无）

返回值
指向首元素的逆向迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::rend",vector,reverse_iterator rend();,"返回指向逆向的 vector 末元素后一元素的逆向迭代器。它对应非逆向 vector 首元素的前一元素。此元素表现为占位符，试图访问它导致未定义行为。参数
（无）

返回值
指向末元素后一元素的逆向迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::rend",vector,const_reverse_iterator rend() const;,"返回指向逆向的 vector 末元素后一元素的逆向迭代器。它对应非逆向 vector 首元素的前一元素。此元素表现为占位符，试图访问它导致未定义行为。参数
（无）

返回值
指向末元素后一元素的逆向迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::crend",vector,const_reverse_iterator crend() const noexcept;,"返回指向逆向的 vector 末元素后一元素的逆向迭代器。它对应非逆向 vector 首元素的前一元素。此元素表现为占位符，试图访问它导致未定义行为。参数
（无）

返回值
指向末元素后一元素的逆向迭代器。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::empty",vector,bool empty() const;,"检查容器是否无元素，即是否 begin() == end()。

参数
（无）

返回值
若容器为空则为 true，否则为 false。","{
  transitive(<-1>, <0>)
}","{
    <-1> = 0 || 1
}"
"std::vector<T,Allocator>::size",vector,size_type size() const;,"返回容器中的元素数，即 std::distance(begin(), end())。

参数
（无）

返回值
容器中的元素数量。","{
  transitive(<-1>, <0>)
}","{
    <-1> = <0>.size
}"
"std::vector<T,Allocator>::max_size",vector,size_type max_size() const;,"返回容器根据系统或库实现限制而可保有的元素最大数量，即对于最大容器的 std::distance(begin(), end())。

参数
（无）

返回值
元素数量的最大值。",{},"{
    <-1> = [0, MAX]
}"
"std::vector<T,Allocator>::reserve",vector,void reserve( size_type new_cap );,"增加 vector 的容量（即 vector 在不重新分配存储的情况下能最多能持有的元素的数量）到大于或等于 new_cap 的值。如果 new_cap 大于当前的 capacity()，那么就会分配新存储，否则该方法不做任何事。

reserve() 不会更改 vector 的大小。

如果 new_cap 大于 capacity()，那么指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。否则，没有迭代器或引用会失效。

在调用 reserve() 后，插入只会在它将导致 vector 的大小大于 capacity() 的值时触发重新分配。

参数
new_cap	-	vector 的新容量
类型要求
-T 必须符合可移动插入 (MoveInsertable) 到 *this 中的要求。(C++11 起)
返回值
（无）","{
  setSink(<1>)
}","{
    expansion(<0>)
}"
"std::vector<T,Allocator>::capacity",vector,size_type capacity() const;,"返回容器当前已为之分配空间的元素数。

参数
（无）

返回值
当前分配存储的容量。","{
  transitive(<-1>, <0>)
}","{
    <-1> = [<0>.size, MAX]
}"
"std::vector<T,Allocator>::shrink_to_fit",vector,void shrink_to_fit();,"请求移除未使用的容量。

它是减少 capacity() 到 size()非强制性请求。请求是否达成依赖于实现。

如果发生重分配，那么指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。如果没有发生重分配，那么没有迭代器或引用会失效。

如果 T 不可移动插入 (MoveInsertable) 到 std::vector<T, Allocator> 中，那么行为未定义。",{},{}
"std::vector<T,Allocator>::clear",vector,void clear();,"从容器擦除所有元素。此调用后 size() 返回零。

使任何指代所含元素的引用、指针和迭代器失效。 任何尾后迭代器也会失效。

保持 vector 的 capacity() 不变","{
  sanitize(<0>)
}","{
    <0>.size = 0
    <0>.field = NULL
}"
"std::vector<T,Allocator>::insert",vector,"iterator insert( const_iterator pos, const T& value );","在 pos 前插入 value 的副本。参数
pos	-	将内容插入到它前面的迭代器。pos 可以是 end() 迭代器
value	-	要插入的元素值. 返回值
1,2) 指向被插入 value 的迭代器。","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    <0>.field = <2>
    <0>.size = <0>.size + 1
    expansion(<0>)
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::insert",vector,"iterator insert( const_iterator pos, T&& value );","在 pos 前插入 value，可能使用移动语义。参数
pos	-	将内容插入到它前面的迭代器。pos 可以是 end() 迭代器
value	-	要插入的元素值. 指向被插入 value 的迭代器。","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    <0>.field = <2>
    <0>.size = <0>.size + 1
    expansion(<0>)
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::insert",vector,"iterator insert( const_iterator pos,
                 size_type count, const T& value );"," 在 pos 前插入 value 的 count 个副本。参数
pos	-	将内容插入到它前面的迭代器。pos 可以是 end() 迭代器
value	-	要插入的元素值
count	-	要插入的元素数量 。指向首个被插入元素的迭代器，或者在 count == 0 时返回 pos。","{
  setSink(<2>)
  transitive(<0>, <0> || <3>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    <0>.field = <3>
    <0>.size = <0>.size + <2>
    expansion(<0>)
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::insert",vector,"template< class InputIt >
iterator insert( const_iterator pos, InputIt first, InputIt last );","在 pos 前插入来自范围 [first, last) 的元素。参数
pos	-	将内容插入到它前面的迭代器。pos 可以是 end() 迭代器
value	-	要插入的元素值
count	-	要插入的元素数量
first, last	-	要插入的（不能是指向调用 insert 所用的容器中的迭代器）元素范围的迭代器对。 指向首个被插入元素的迭代器，或者在 first == last 时返回 pos。","{
  transitive(<0>, <0> || <1> || <2>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    <0>.field = <2>.field
    <0>.size = [<0>.size, MAX]
    expansion(<0>)
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::insert",vector,"iterator insert( const_iterator pos, std::initializer_list<T> ilist );","在 pos 前插入来自初始化器列表 ilist 的元素。参数
pos	-	将内容插入到它前面的迭代器。pos 可以是 end() 迭代器
value	-	要插入的元素值
count	-	要插入的元素数量
first, last	-	要插入的（不能是指向调用 insert 所用的容器中的迭代器）元素范围的迭代器对
ilist	-	要插入的值来源的 std::initializer_list。 返回值指向首个被插入元素的迭代器，或者在 ilist 为空时返回 pos。","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    <0>.size = <0>.size + <2>.size
    <0>.field = <2>.field
    expansion(<0>)
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::emplace",vector,"template< class... Args >
iterator emplace( const_iterator pos, Args&&... args );","在紧接 pos 之前的位置向容器插入新元素。

通过 std::allocator_traits::construct 构造元素，常用布置 new 在容器提供的位置原位构造元素。然而如果要求的位置已被既存的元素占据，那么首先在另一位置构造被插入的元素，然后再将它移动赋值到要求的位置中。

将实参 args... 作为 std::forward<Args>(args)... 转发给构造函数。 args... 可以直接或间接地指代容器中的值。

如果操作后新的 size() 大于原 capacity() 则会发生重分配，这种情况下，指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。否则，仅插入点之前的迭代器和引用保持有效。

参数
pos	-	将构造新元素到其前的迭代器
args	-	转发给元素构造函数的参数
类型要求
-T 必须满足可移动赋值 (MoveAssignable) 、可移动插入 (MoveInsertable) 和 可就位构造 (EmplaceConstructible) 。
返回值
指向被安置的元素的迭代器。","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    <0>.size = <0>.size + 1
    <0>.field = <2>
    expansion(<0>)
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::erase",vector,iterator erase( iterator pos );,"移除位于 pos 的元素。参数
pos	-	指向要移除的元素的迭代器。返回值
最后移除元素之后的迭代器。

1) 如果 pos 指代末元素，那么返回 end() 迭代器。","{
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end - 1])
    <0>.size = <0>.size - 1
    <0>.field = NULL
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::erase",vector,iterator erase( const_iterator pos );,"移除位于 pos 的元素。参数
pos	-	指向要移除的元素的迭代器。返回值
最后移除元素之后的迭代器。

1) 如果 pos 指代末元素，那么返回 end() 迭代器。","{
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end - 1])
    <0>.size = <0>.size - 1
    <0>.field = NULL
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::erase",vector,"iterator erase( iterator first, iterator last );","移除范围 [first, last) 中的元素。
指向位于擦除点或其后的元素的迭代器（包括 end() 迭代器）和引用均会失效。

迭代器 pos 必须合法且可解引用，因此不能以 end() 迭代器（合法，但不可解引用）作为 pos 的值。

如果 first == last，那么迭代器 first 不需要可解引用：擦除空范围是无操作。参数
pos	-	指向要移除的元素的迭代器
first, last	-	要移除的元素范围的迭代器对。返回值
最后移除元素之后的迭代器。

1) 如果 pos 指代末元素，那么返回 end() 迭代器。
2) 如果在移除前 last == end()，那么返回更新的 end() 迭代器。
 如果范围 [first, last) 为空，那么返回 last。","{
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    overflow(<2>, [<0>.begin, <0>.end])
    <0>.size = [0, <0>.size]
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::erase",vector,"iterator erase( const_iterator first, const_iterator last );","移除范围 [first, last) 中的元素。
指向位于擦除点或其后的元素的迭代器（包括 end() 迭代器）和引用均会失效。

迭代器 pos 必须合法且可解引用，因此不能以 end() 迭代器（合法，但不可解引用）作为 pos 的值。

如果 first == last，那么迭代器 first 不需要可解引用：擦除空范围是无操作。参数
pos	-	指向要移除的元素的迭代器
first, last	-	要移除的元素范围的迭代器对。返回值
最后移除元素之后的迭代器。

1) 如果 pos 指代末元素，那么返回 end() 迭代器。
2) 如果在移除前 last == end()，那么返回更新的 end() 迭代器。
 如果范围 [first, last) 为空，那么返回 last。","{
  transitive(<-1>, <0>)
}","{
    overflow(<1>, [<0>.begin, <0>.end])
    overflow(<2>, [<0>.begin, <0>.end])
    <0>.size = [0, <0>.size]
    <-1> = [<0>.begin, <0>.end]
}"
"std::vector<T,Allocator>::push_back",vector,void push_back( const T& value );,"追加给定元素 value 到容器尾。

1) 初始化新元素为 value 的副本。如果操作后新的 size() 大于原 capacity() 则会发生重分配，这种情况下，指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。否则仅有 end() 迭代器失效。参数
value	-	要追加的元素值","{
  transitive(<0>, <0>  || <1>)
}","{
    <0>.size = <0>.size + 1
    <0>.field = <1>
    expansion(<0>)
}"
"std::vector<T,Allocator>::push_back",vector,void push_back( T&& value );,"追加给定元素 value 到容器尾。移动 value 进新元素。
如果操作后新的 size() 大于原 capacity() 则会发生重分配，这种情况下，指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。否则仅有 end() 迭代器失效。参数
value	-	要追加的元素值","{
  transitive(<0>, <0> || <1>)
}","{
    <0>.size = <0>.size + 1
    <0>.field = <1>
    expansion(<0>)
}"
"std::vector<T,Allocator>::emplace_back",vector,"template< class... Args >
void emplace_back( Args&&... args );","添加新元素到容器尾。元素通过 std::allocator_traits::construct 构造，通常用布置 new 于容器所提供的位置原位构造元素。实参 args... 以 std::forward<Args>(args)... 转发到构造函数。

如果操作后新的 size() 大于原 capacity() 则会发生重分配，这种情况下，指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。否则仅有 end() 迭代器失效。

参数
args	-	转发到元素构造函数的实参","{
  transitive(<0>, <0> || <1>)
}","{
    <0>.size = <0>.size + 1
    <0>.field = <1>
    expansion(<0>)
}"
"std::vector<T,Allocator>::emplace_back",vector,"template< class... Args >
reference emplace_back( Args&&... args );","添加新元素到容器尾。元素通过 std::allocator_traits::construct 构造，通常用布置 new 于容器所提供的位置原位构造元素。实参 args... 以 std::forward<Args>(args)... 转发到构造函数。

如果操作后新的 size() 大于原 capacity() 则会发生重分配，这种情况下，指代元素的所有迭代器（包括 end() 迭代器）和所有引用均会失效。否则仅有 end() 迭代器失效。

参数
args	-	转发到元素构造函数的实参  返回值 到被插入元素的引用。返回值：到被插入元素的引用。","{
  transitive(<0>, <0> || <1>)
  transitive(<-1>, <0>)
}","{
    <0>.size = <0>.size + 1
    <0>.field = <1>
    expansion(<0>)
    <-1> = <0>.field
}"
"std::vector<T,Allocator>::pop_back",vector,void pop_back();,"移除容器的末元素。

在空容器上调用 pop_back 导致未定义行为。

指向最后元素的迭代器和引用失效。end() 迭代器也失效。

参数
（无）

返回值
（无）","{
}","{
    <0>.size = <0>.size - 1
}"
"std::vector<T,Allocator>::resize",vector,void resize( size_type count );,"重设容器大小以容纳 count 个元素，在 count == size() 时不做任何事。

如果当前大小大于 count，那么减小容器到它的前 count 个元素。

如果当前大小小于 count，那么：

1) 追加额外的默认插入的元素。参数
count	-	容器的大小
","{
  setSink(<1>)
}","{
    <0>.size = <1>
    expansion(<0>)
}"
"std::vector<T,Allocator>::resize",vector,"void resize( size_type count, const value_type& value );","重设容器大小以容纳 count 个元素，在 count == size() 时不做任何事。

如果当前大小大于 count，那么减小容器到它的前 count 个元素。

如果当前大小小于 count，那么：追加额外的 value 的副本。参数
count	-	容器的大小
value	-	用以初始化新元素的值","{
  setSink(<1>)
  transitive(<0>, <0> || <2>)
}","{
    <0>.size = <1>
    <0>.field = <2>
    expansion(<0>)
}"
"std::vector<T,Allocator>::swap",vector,void swap( vector& other );,"将内容以及容量与 other 的交换。不在单独的元素上调用任何移动、复制或交换操作。

所有迭代器和引用仍然有效。end() 迭代器失效。参数
other	-	要与之交换内容的容器","{
  swapTaint(<0>, <1>)
}","{
    swap(<0>, <1>)
}"
"std::vector<T,Allocator>::swap",vector,void swap( vector& other ) noexcept,"将内容以及容量与 other 的交换。不在单独的元素上调用任何移动、复制或交换操作。

所有迭代器和引用仍然有效。end() 迭代器失效。参数
other	-	要与之交换内容的容器","{
  swapTaint(<0>, <1>)
}","{
    swap(<0>, <1>)
}"
"std::vector<T,Allocator>::swap",vector,constexpr void swap( vector& other ) noexcept,"将内容以及容量与 other 的交换。不在单独的元素上调用任何移动、复制或交换操作。

所有迭代器和引用仍然有效。end() 迭代器失效。参数
other	-	要与之交换内容的容器","{
  swapTaint(<0>, <1>)
}","{
    swap(<0>, <1>)
}"
"operator==,!=,<,<=,>,>=,<=>(std::vector)",,"template< class T, class Alloc >
bool operator==( const std::vector<T, Alloc>& lhs,

                 const std::vector<T, Alloc>& rhs );","比较两个 vector 的内容。

1,2) 检查 lhs 与 rhs 的内容是否相等，即它们是否拥有相同数量的元素且 lhs 中每个元素与 rhs 的同位置元素比较相等。参数
lhs, rhs	-	要比较内容的 vector
- 为使用重载 (1,2)， T 必须满足可相等比较 (EqualityComparable) 。
- 为使用重载 (3-6)， T 必须满足可小于比较 (LessThanComparable) 。顺序关系必须建立全序。
返回值
1) 在 vector 内容相等时返回 true，否则返回 false",{},"{
    <-1> = 0 || 1
}"
"operator==,!=,<,<=,>,>=,<=>(std::vector)",,"template< class T, class Alloc >
bool operator!=( const std::vector<T, Alloc>& lhs,

                 const std::vector<T, Alloc>& rhs );","比较两个 vector 的内容。

1,2) 检查 lhs 与 rhs 的内容是否相等，即它们是否拥有相同数量的元素且 lhs 中每个元素与 rhs 的同位置元素比较相等。参数
lhs, rhs	-	要比较内容的 vector。返回值： 在 vector 内容不相等时返回 true，否则返回 false",{},"{
    <-1> = 0 || 1
}"
"operator==,!=,<,<=,>,>=,<=>(std::vector)",,"template< class T, class Alloc >
bool operator<( const std::vector<T, Alloc>& lhs,

                const std::vector<T, Alloc>& rhs );","比较两个 vector 的内容。按字典序比较 lhs 与 rhs 的内容。由等价于 std::lexicographical_compare 的函数进行比较。参数
lhs, rhs	-	要比较内容的 vector。返回值在 lhs 的内容按字典序小于 rhs 的内容时返回 true，否则返回 false",{},"{
    <-1> = 0 || 1
}"
"operator==,!=,<,<=,>,>=,<=>(std::vector)",,"template< class T, class Alloc >
bool operator<=( const std::vector<T, Alloc>& lhs,

                 const std::vector<T, Alloc>& rhs );","比较两个 vector 的内容。按字典序比较 lhs 与 rhs 的内容。由等价于 std::lexicographical_compare 的函数进行比较。参数
lhs, rhs	-	要比较内容的 vector。返回值在 lhs 的内容按字典序小于 ﻿或等于 rhs 的内容时返回 true，否则返回 false",{},"{
    <-1> = 0 || 1
}"
"operator==,!=,<,<=,>,>=,<=>(std::vector)",,"template< class T, class Alloc >
bool operator>( const std::vector<T, Alloc>& lhs,

                const std::vector<T, Alloc>& rhs );","比较两个 vector 的内容。按字典序比较 lhs 与 rhs 的内容。由等价于 std::lexicographical_compare 的函数进行比较。参数
lhs, rhs	-	要比较内容的 vector。返回值在 lhs 的内容按字典序大于 rhs 的内容时返回 true，否则返回 false",{},"{
    <-1> = 0 || 1
}"
"operator==,!=,<,<=,>,>=,<=>(std::vector)",,"template< class T, class Alloc >
bool operator>=( const std::vector<T, Alloc>& lhs,

                 const std::vector<T, Alloc>& rhs );","比较两个 vector 的内容。按字典序比较 lhs 与 rhs 的内容。由等价于 std::lexicographical_compare 的函数进行比较。参数
lhs, rhs	-	要比较内容的 vector。返回值在 lhs 的内容按字典序大于 ﻿或等于 rhs 的内容时返回 true，否则返回 false",{},"{
    <-1> = 0 || 1
}"
"operator==,!=,<,<=,>,>=,<=>(std::vector)",,"template< class T, class Alloc >
constexpr synth-three-way-result<T>
    operator<=>( const std::vector<T, Alloc>& lhs,

                 const std::vector<T, Alloc>& rhs );","按字典序比较 lhs 与 rhs 的内容。如同通过调用 std::lexicographical_compare_three_way(lhs.begin(), lhs.end(),
                                       rhs.begin(), rhs.end(), synth-three-way) 进行比较。
 返回类型是 synth-three-way 的返回类型（即 synth-three-way-result ﻿<T>）。
 如果不满足以下任意条件，那么行为未定义：
T 实现了 three_way_comparable。
为（可有 const 限定的）T 类型的值定义了 <，并且 < 是全序关系。参数：参数
lhs, rhs	-	要比较内容的 vector， 返回值：lhs 与 rhs 中的首对不等价元素的相对顺序，如果有这种元素；否则是 lhs.size() <=> rhs.size()。",{},{}
std::swap,,"template< class T, class Alloc >
void swap( std::vector<T, Alloc>& lhs,

           std::vector<T, Alloc>& rhs );","为 std::vector 特化了 std::swap 算法。交换 lhs 与 rhs 的内容。调用 lhs.swap(rhs)。



参数
lhs, rhs	-	要交换内容的容器
返回值
（无）","{
  swapTaint(<1>, <2>)
}","{
    swap(<1>, <2>)
}"
std::swap,,"template< class T, class Alloc >
void swap( std::vector<T, Alloc>& lhs,
           std::vector<T, Alloc>& rhs )

               noexcept","为 std::vector 特化了 std::swap 算法。交换 lhs 与 rhs 的内容。调用 lhs.swap(rhs)。



参数
lhs, rhs	-	要交换内容的容器
返回值
（无）","{
  swapTaint(<1>, <2>)
}","{
    swap(<1>, <2>)
}"
std::erase,,"template< class T, class Alloc, class U >
constexpr std::vector<T, Alloc>::size_type

    erase( std::vector<T, Alloc>& c, const U& value );","从容器中擦除所有比较等于 value 的元素。等价于
auto it = std::remove(c.begin(), c.end(), value);
auto r = c.end() - it;
c.erase(it, c.end());
return r; 参数
c	-	要从中擦除的容器
value	-	要擦除的值 。返回值
被擦除的元素数。","{
    transitive(<-1>, <1>)
}","{
    <-1> = [0, <1>.size]
    <1>.size = [0, <1>.size]
}"
std::erase,,"template< class T, class Alloc, class U = T >
constexpr std::vector<T, Alloc>::size_type

    erase( std::vector<T, Alloc>& c, const U& value );","从容器中擦除所有比较等于 value 的元素。等价于
auto it = std::remove(c.begin(), c.end(), value);
auto r = c.end() - it;
c.erase(it, c.end());
return r; 参数
c	-	要从中擦除的容器
value	-	要擦除的值 。返回值
被擦除的元素数。","{
    transitive(<-1>, <1>)
}","{
    <-1> = [0, <1>.size]
    <1>.size = [0, <1>.size]
}"
std::erase_if,,"template< class T, class Alloc, class Pred >
constexpr std::vector<T, Alloc>::size_type

    erase_if( std::vector<T, Alloc>& c, Pred pred );","从容器中擦除所有满足 pred 的元素。等价于
auto it = std::remove_if(c.begin(), c.end(), pred);
auto r = c.end() - it;
c.erase(it, c.end());
return r; 参数
c	-	要从中擦除的容器
value	-	要擦除的值
pred	-	若应该擦除元素则返回 ​true 的一元谓词。
对每个（可为 const 的） T 类型参数 v ，表达式 pred(v) 必须可转换到 bool，无关乎值类别，而且必须不修改 v 。从而不允许 T& 类型参数，亦不允许 T ，除非对 T 而言移动等价于复制(C++11 起)。​

返回值
被擦除的元素数。","{
    transitive(<-1>, <1>)
}","{
    <-1> = [0, <1>.size]
    <1>.size = [0, <1>.size]
}"
"std::basic_string<CharT,Traits,Allocator>::reserve",basic_string,void reserve( size_type new_cap = 0 );,"告知 std::basic_string 对象大小的有计划更改，使得它能准确地管理存储分配。
如果 new_cap 大于当前 capacity()，那么分配新存储，并使 capacity() 大于或等于 new_cap。
如果 new_cap 小于当前 capacity()，那么这是非强制的收缩请求。
如果 new_cap 小于当前 size()，那么这是非强制的收缩到适合（shrink-to-fit）请求，等价于 shrink_to_fit()(C++11 起)。 (C++20 前)
如果 new_cap 小于或等于当前 capacity()，那么没有效果。 (C++20 起)
如果容量有更改，那么所有迭代器与引用（包含尾后迭代器）都会失效。

 参数
new_cap	-	字符串的新容量

返回值
无","{
  setSink(<1>)
}","{
  expansion(<0>)
}"
"std::basic_string<CharT,Traits,Allocator>::reserve",basic_string,constexpr void reserve( size_type new_cap );,"告知 std::basic_string 对象大小的有计划更改，使得它能准确地管理存储分配。
如果 new_cap 大于当前 capacity()，那么分配新存储，并使 capacity() 大于或等于 new_cap。
如果 new_cap 小于当前 capacity()，那么这是非强制的收缩请求。
如果 new_cap 小于当前 size()，那么这是非强制的收缩到适合（shrink-to-fit）请求，等价于 shrink_to_fit()(C++11 起)。 (C++20 前)
如果 new_cap 小于或等于当前 capacity()，那么没有效果。 (C++20 起)
如果容量有更改，那么所有迭代器与引用（包含尾后迭代器）都会失效。

 参数
new_cap	-	字符串的新容量

返回值
无","{
  setSink(<1>)
}","{
  expansion(<0>)
}"
"std::basic_string<CharT,Traits,Allocator>::reserve",basic_string,void reserve();,"非强制的收缩到适合请求。调用后 capacity() 拥有大于或等于 size() 的未指定值。

 参数
无

返回值
无",{},{}
"std::basic_string<CharT,Traits,Allocator>::capacity",basic_string,size_type capacity() const;,"返回当前已为字符串分配空间的字符数。

 参数
无

返回值
当前分配的存储，即可用于存储元素的存储的容量。","{
  transitive(<-1>, <0>)
}","{
  <-1> = [<0>.size, MAX]
}"
"std::basic_string<CharT,Traits,Allocator>::shrink_to_fit",basic_string,void shrink_to_fit();,"请求移除未使用的容量。
这是减少 capacity() 到 size() 的非强制请求。是否满足请求取依赖于实现。
当且仅当发生重分配时使所有指针、引用和迭代器失效。

 参数
无

返回值
无",{},{}
"std::basic_string<CharT,Traits,Allocator>::clear",basic_string,void clear();,"如同通过执行 erase(begin(), end()) 从字符串中移除所有字符。
使所有指针、引用及迭代器失效。

 参数
无

返回值
无","{
  sanitize(<0>)
}","{
  <0>.strlen = 0
  <0>.size = <0>.strlen + 1
  <0>.field = NULL
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"basic_string& insert( size_type index, size_type count, CharT ch );","插入字符到字符串中。
在位置 index 插入 count 个字符 ch 的副本。

 参数
index	-	插入内容到的位置
pos	-	将插入字符到它之前的迭代器
ch	-	要插入的字符

返回值
*this","{
  setSink(<1>)
  setSink(<2>)
  transitive(<0>, <0> || <3>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  <0>.strlen = <0>.strlen + <2>
  <0>.size = <0>.strlen + 1
  <0>.field = <3>
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"basic_string& insert( size_type index, const CharT* s );","插入字符到字符串中。
在位置 index 插入 s 所指向的空终止字符串。字符串的长度由首个空字符，通过 Traits::length(s) 确定。

 参数
index	-	插入内容到的位置
pos	-	将插入字符到它之前的迭代器
ch	-	要插入的字符
s	-	指向要插入的字符串的指针

返回值
*this","{
  setSink(<1>)
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  <0>.strlen = <0>.strlen + <2>.strlen
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"basic_string& insert( size_type index, const CharT* s, size_type count );","插入字符到字符串中。
在位置 index 插入范围 [s, s + count) 中的字符。范围可以包含空字符。

 参数
index	-	插入内容到的位置
pos	-	将插入字符到它之前的迭代器
ch	-	要插入的字符
s	-	指向要插入的字符串的指针
count	-	要插入的字符数

返回值
*this","{
  setSink(<1>)
  setSink(<3>)
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  overflow(<3>, [0, <2>.size])
  <0>.strlen = <0>.strlen + <3>
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"basic_string& insert( size_type index, const basic_string& str );","插入字符到字符串中。
在位置 index 插入字符串 str。

 参数
index	-	插入内容到的位置
str	-	要插入的字符串

返回值
*this","{
  setSink(<1>)
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  <0>.strlen = <0>.strlen + <2>.strlen
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"basic_string& insert( size_type index, const basic_string& str,
                      size_type s_index, size_type count );","插入字符到字符串中。
 在位置 index 插入由 str.substr(s_index, count) 获得的字符串。

 参数
index	-	插入内容到的位置
str	-	要插入的字符串
s_index	-	str 中要插入的首字符位置
count	-	要插入的字符数

返回值
*this","{
  setSink(<1>)
  setSink(<3>)
  setSink(<4>)
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  overflow(<3>, [0, <2>.strlen - 1])
  <0>.strlen = <0>.strlen + <4> || <0>.strlen + <2>.strlen - <3>
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"basic_string& insert( size_type index, const basic_string& str,
                      size_type s_index, size_type count = npos );","插入字符到字符串中。
 在位置 index 插入由 str.substr(s_index, count) 获得的字符串。

 参数
index	-	插入内容到的位置
str	-	要插入的字符串
s_index	-	str 中要插入的首字符位置
count	-	要插入的字符数

返回值
*this","{
  setSink(<1>)
  setSink(<3>)
  setSink(<4>)
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  overflow(<3>, [0, <2>.strlen - 1])
  <0>.strlen = <0>.strlen + <4> || <0>.strlen + <2>.strlen - <3>
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"iterator insert( iterator pos, CharT ch );","插入字符到字符串中。
在 pos 所指向的字符前插入字符 ch。

 参数
pos	-	将插入字符到它之前的迭代器
ch	-	要插入的字符

返回值
指代首个被插入字符的迭代器，或者在未插入字符时就是 pos（count == 0，ilist.size() == 0 或 first == last）","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = <0>.strlen + 1
  <0>.size = <0>.strlen + 1
  <0>.field = <2>
  expansion(<0>)
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"iterator insert( const_iterator pos, CharT ch );","插入字符到字符串中。
在 pos 所指向的字符前插入字符 ch。

 参数
pos	-	将插入字符到它之前的迭代器
ch	-	要插入的字符

返回值
指代首个被插入字符的迭代器，或者在未插入字符时就是 pos（count == 0，ilist.size() == 0 或 first == last）","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = <0>.strlen + 1
  <0>.size = <0>.strlen + 1
  <0>.field = <2>
  expansion(<0>)
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"void insert( iterator pos, size_type count, CharT ch );","插入字符到字符串中。
在 pos 所指向的元素（如果存在）前插入 count 个字符 ch 的副本。

 参数
pos	-	将插入字符到它之前的迭代器
ch	-	要插入的字符
count	-	要插入的字符数

返回值
无","{
  setSink(<2>)
  transitive(<0>, <0> || <3>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = <0>.strlen + <2>
  <0>.size = <0>.strlen + 1
  <0>.field = <3>
  expansion(<0>)
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"iterator insert( const_iterator pos, size_type count, CharT ch );","插入字符到字符串中。
在 pos 所指向的元素（如果存在）前插入 count 个字符 ch 的副本。

 参数
pos	-	将插入字符到它之前的迭代器
ch	-	要插入的字符
count	-	要插入的字符数

返回值
指代首个被插入字符的迭代器，或者在未插入字符时就是 pos（count == 0，ilist.size() == 0 或 first == last）","{
  setSink(<2>)
  transitive(<0>, <0> || <3>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = <0>.strlen + <2>
  <0>.size = <0>.strlen + 1
  <0>.field = <3>
  expansion(<0>)
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"template< class InputIt >
void insert( iterator pos, InputIt first, InputIt last );","插入字符到字符串中。
如同用 insert(pos - begin(), basic_string(first, last, get_allocator())) 在 pos 所指向的元素（如果存在）前插入来自范围 [first, last) 的元素。
如果 InputIt 不满足老式输入迭代器 (LegacyInputIterator) ，那么此重载不参与重载决议。(C++11 起)

 参数
pos	-	将插入字符到它之前的迭代器
first, last	-	定义要插入字符的范围

返回值
无","{
  transitive(<0>, <0> || <2> || <3>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = [<0>.strlen, MAX]
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"template< class InputIt >
iterator insert( const_iterator pos, InputIt first, InputIt last );","插入字符到字符串中。
如同用 insert(pos - begin(), basic_string(first, last, get_allocator())) 在 pos 所指向的元素（如果存在）前插入来自范围 [first, last) 的元素。
如果 InputIt 不满足老式输入迭代器 (LegacyInputIterator) ，那么此重载不参与重载决议。(C++11 起)

 参数
pos	-	将插入字符到它之前的迭代器
first, last	-	定义要插入字符的范围

返回值
指代首个被插入字符的迭代器，或者在未插入字符时就是 pos（count == 0，ilist.size() == 0 或 first == last）","{
  transitive(<0>, <0> || <2> || <3>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = [<0>.strlen, MAX]
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"iterator insert( const_iterator pos, std::initializer_list<CharT> ilist );","插入字符到字符串中。
 在 pos 所指向的元素（如果存在）前插入来自初始化式列表 ilist 的字符。

 参数
pos	-	将插入字符到它之前的迭代器
ilist	-	要插入的字符来源的 std::initializer_list

返回值
指代首个被插入字符的迭代器，或者在未插入字符时就是 pos（count == 0，ilist.size() == 0 或 first == last）","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = <0>.strlen + <2>.size - 1
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"template< class StringViewLike >
basic_string& insert( size_type index, const StringViewLike& t );","插入字符到字符串中。
如同用 std::basic_string_view<CharT, Traits> sv = t; 将 t 隐式转换到字符串视图 sv ，然后如同用 insert(pos, sv.data(), sv.size()) 在 pos 所指向的元素（如果存在）前插入来自 sv 的元素。
 此重载只有在 std::is_convertible_v<const StringViewLike&,
                      std::basic_string_view<CharT, Traits>> 是 true 且 std::is_convertible_v<const StringViewLike&, const CharT*> 是 false 时才会参与重载决议。

 参数
index	-	插入内容到的位置
t	-	要插入的字符来源对象（可转换到 std::basic_string_view）
count	-	要插入的字符数

返回值
*this","{
  setSink(<1>)
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  <0>.strlen = <0>.strlen + <2>.strlen
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert",basic_string,"template< class StringViewLike >
basic_string& insert( size_type index, const StringViewLike& t,
                      size_type t_index, size_type count = npos );","插入字符到字符串中。
如同用 std::basic_string_view<CharT, Traits> sv = t; 将 t 隐式转换到字符串视图 sv ，然后在 pos 所指向的元素（如果存在）前插入来自 sv 的子视图 [t_index, t_index + count) 的元素。
如果请求的子视图越过 sv 的末尾或 count == npos，那么作为结果的子视图是 [t_index, sv.size())。
如果 t_index > sv.size() 或 index > size()，那么抛出 std::out_of_range。
 此重载只有在 std::is_convertible_v<const StringViewLike&,
                      std::basic_string_view<CharT, Traits>> 是 true 且 std::is_convertible_v<const StringViewLike&, const CharT*> 是 false 时才会参与重载决议。
如果 pos 不是 *this 上的有效迭代器，那么行为未定义。

 参数
index	-	插入内容到的位置
t	-	要插入的字符来源对象（可转换到 std::basic_string_view）
count	-	要插入的字符数
t_index	-	t 中要插入的首字符位置

返回值
*this","{
  setSink(<1>)
  setSink(<3>)
  setSink(<4>)
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  overflow(<3>, [0, <2>.strlen - 1])
  <0>.strlen = <0>.strlen + <4> || <0>.strlen + <2>.strlen - <3>
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::insert_range",basic_string,"template< container-compatible-range<CharT> R >
constexpr iterator insert_range( const_iterator pos, R&& rg );","向 pos 所指向的元素（如果有）之前插入范围 rg 中的字符。

等价于

return insert(pos - begin(),
    std::basic_string(
        std::from_range,
        std​::​forward<R>(rg),
        get_allocator())
);
如果 pos 不是 *this 上的有效迭代器，则其行为未定义。

 参数
pos	-	迭代器，将在它之前插入字符
rg	-	容器兼容范围

返回值
指代插入的第一个字符的迭代器，或者当 rg 为空而没有插入任何字符时为 pos。","{
  transitive(<0>, <0> || <2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  <0>.strlen = [<0>.strlen, MAX]
  <0>.size = <0>.strlen + 1
  <0>.field = <2>.field
  expansion(<0>)
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,"basic_string& erase( size_type index = 0, size_type count = npos );","从字符串移除指定的字符。
移除从 index 开始的 std::min(count, size() - index) 个字符。

 参数
index	-	要移除的首个字符
count	-	要移除的字符数

返回值
*this","{
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  <0>.strlen = <0>.strlen - <2> || <1>
  <0>.size = <0>.strlen + 1
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,"constexpr basic_string& erase( size_type index = 0, size_type count = npos );","从字符串移除指定的字符。
移除从 index 开始的 std::min(count, size() - index) 个字符。

 参数
index	-	要移除的首个字符
count	-	要移除的字符数

返回值
*this","{
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [0, <0>.strlen])
  <0>.strlen = <0>.strlen - <2> || <1>
  <0>.size = <0>.strlen + 1
  <-1> = <0>
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,iterator erase( iterator position );,"从字符串移除指定的字符。
移除位于 position 的字符。
 如果 position 不是 *this 上的可解引用迭代器，那么行为未定义。

 参数
position	-	指向要移除的字符的迭代器

返回值
指向紧跟被擦除字符之后的迭代器，或者在不存在这种字符的情况下返回 end()。","{
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end - 1])
  <0>.strlen = <0>.strlen - 1
  <0>.size = <0>.strlen + 1
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,iterator erase( const_iterator position );,"从字符串移除指定的字符。
移除位于 position 的字符。
 如果 position 不是 *this 上的可解引用迭代器，那么行为未定义。

 参数
position	-	指向要移除的字符的迭代器

返回值
指向紧跟被擦除字符之后的迭代器，或者在不存在这种字符的情况下返回 end()。","{
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end - 1])
  <0>.strlen = <0>.strlen - 1
  <0>.size = <0>.strlen + 1
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,constexpr iterator erase( const_iterator position );,"从字符串移除指定的字符。
移除位于 position 的字符。
 如果 position 不是 *this 上的可解引用迭代器，那么行为未定义。

 参数
position	-	指向要移除的字符的迭代器

返回值
指向紧跟被擦除字符之后的迭代器，或者在不存在这种字符的情况下返回 end()。","{
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end - 1])
  <0>.strlen = <0>.strlen - 1
  <0>.size = <0>.strlen + 1
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,"iterator erase( iterator first, iterator last );","从字符串移除指定的字符。
移除范围 [first, last) 中的字符。
 如果 first 或 last 不是 *this 上的有效迭代器，或者 [first, last) 不是有效范围，那么行为未定义。

 参数
first, last	-	要移除的字符范围

返回值
指向擦除前 last 指向的字符的迭代器，或者在不存在这种字符的情况下返回 end()。","{
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  overflow(<2>, [<0>.begin, <0>.end])
  <0>.strlen = [0, <0>.strlen]
  <0>.size = <0>.strlen + 1
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,"iterator erase( const_iterator first, const_iterator last );","从字符串移除指定的字符。
移除范围 [first, last) 中的字符。
 如果 first 或 last 不是 *this 上的有效迭代器，或者 [first, last) 不是有效范围，那么行为未定义。

 参数
first, last	-	要移除的字符范围

返回值
指向擦除前 last 指向的字符的迭代器，或者在不存在这种字符的情况下返回 end()。","{
  setSink(<1>)
  setSink(<2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  overflow(<2>, [<0>.begin, <0>.end])
  <0>.strlen = [0, <0>.strlen]
  <0>.size = <0>.strlen + 1
  <-1> = [<0>.begin, <0>.end]
}"
"std::basic_string<CharT,Traits,Allocator>::erase",basic_string,"constexpr iterator erase( const_iterator first, const_iterator last );","从字符串移除指定的字符。
移除范围 [first, last) 中的字符。
 如果 first 或 last 不是 *this 上的有效迭代器，或者 [first, last) 不是有效范围，那么行为未定义。

 参数
first, last	-	要移除的字符范围

返回值
指向擦除前 last 指向的字符的迭代器，或者在不存在这种字符的情况下返回 end()。","{
  setSink(<1>)
  setSink(<2>)
  transitive(<-1>, <0>)
}","{
  overflow(<1>, [<0>.begin, <0>.end])
  overflow(<2>, [<0>.begin, <0>.end])
  <0>.strlen = [0, <0>.strlen]
  <0>.size = <0>.strlen + 1
  <-1> = [<0>.begin, <0>.end]
}"
